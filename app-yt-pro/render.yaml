services:
  - type: web
    name: app-yt-pro
    runtime: docker
    dockerContext: .
    dockerfilePath: Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: RENDER
        value: "true"
      - key: GROQ_API_KEY
        sync: false
